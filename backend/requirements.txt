# === Core ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# === Supabase ===
supabase==2.11.0
httpx==0.28.1

# === Stock Data ===
twstock==1.3.1
yfinance==0.2.50

# === Real-time Quote Feeds ===
fugle-marketdata>=1.0.0       # Phase 1: Fugle WebSocket (Taiwan stocks)
websocket-client>=1.6.0       # Phase 2: Finnhub WebSocket (US stocks)
# finnhub-python>=2.4.0       # Optional: Finnhub REST client (not required for WS)
# shioaji>=1.0.0              # Phase 3: Sinopac broker API (optional, large dep)

# === Scheduling ===
apscheduler==3.10.4

# === LINE Bot (Phase 3) ===
# Using httpx (already included) for LINE Messaging API calls
# No additional SDK needed — raw REST API is simpler and lighter

# === Telegram (Phase 4) ===
# Using httpx (already included) for Telegram Bot API calls
# Same lightweight approach as LINE — no SDK needed

# === Utilities ===
python-dotenv==1.0.1
